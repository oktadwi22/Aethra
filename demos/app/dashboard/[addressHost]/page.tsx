// app/dashboard/[hostId]/page.tsx
'use client'

import Dashboard from "@/pages/dashboard/dashboard"
type Props = {
  params: Promise<{ addressHost: string }>
}

export default async function DashboardPage() {
    const 
    return( <Dashboard/>)
}
